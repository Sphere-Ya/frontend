import PastEvents from "../PastEvents/EventsCardList";
import './Events.css';

function Events() {

    return (
        <section className="events">
            <PastEvents />
        </section>
    );
}

export default Events;
